import React, { FC } from "react";
import { Divider } from "@mui/material";

const FullWidthDivider: FC = () => {
  return (
    <Divider
      sx={{
        width: "100%",
      }}
    />
  );
};

export default FullWidthDivider;
